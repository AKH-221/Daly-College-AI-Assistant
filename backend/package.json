{
  "name": "daly-college-chat-backend",
  "version": "1.0.0",
"scripts": {
  "build": "cd backend && npm install && npm run build"
}

    "start": "node dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }

